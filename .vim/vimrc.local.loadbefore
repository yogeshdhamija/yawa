if !exists("added_plugins")
    let added_plugins = []
endif
let added_plugins = added_plugins + [
    \["rust-lang/rust.vim", {}],
\]

let g:rustfmt_autosave = 1

if !exists("added_coc_plugins")
    let added_coc_plugins = []
endif
let added_coc_plugins = added_coc_plugins + ["coc-rust-analyzer", "coc-rls"]

